<% if (typeof user !== 'undefined' && user) { %>
  </div> <!-- container-fluid -->
</div> <!-- main-content -->
<% } else { %>
</main>
<% } %>

<footer class="footer py-3 bg-light border-top mt-auto <% if (typeof user !== 'undefined' && user) { %>sidebar-offset<% } %>">
  <div class="container text-center text-muted small">
    <i class="bi bi-lock-fill me-1"></i>
    Read-only access to <strong>Released</strong> part specifications &mdash; data managed in the PLM system.
  </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/app.js"></script>
</body>
</html>
